version: '3.8'

services:
  # 1. SZÁJ: Piper (Text-to-Speech) 🗣️
  piper:
    image: rhasspy/wyoming-piper
    container_name: wyoming-piper
    restart: unless-stopped
    ports:
      - "10200:10200"
    volumes:
      - ./piper_data:/data
    command: --voice hu_HU-anna-medium

  # 2. FÜL: Whisper (Speech-to-Text) 👂
  whisper:
    image: rhasspy/wyoming-whisper
    container_name: wyoming-whisper
    restart: unless-stopped
    ports:
      - "10300:10300"
    volumes:
      - ./whisper_data:/data
    command: --model tiny-int8 --language hu

  # 3. FIGYELEM: OpenWakeWord (Ébresztő szó) ⏰
  wakeword:
    image: rhasspy/wyoming-openwakeword
    container_name: wyoming-openwakeword
    restart: unless-stopped
    ports:
      - "10400:10400"
    # Nem feltétlenül kell volume, de ha saját modellt tanítasz, jól jöhet
    volumes:
      - ./wakeword_data:/data
    command: --preload-model 'ok_nabu'